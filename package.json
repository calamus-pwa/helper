{
  "author": "CalamusBikes",
  "name": "@calamus-pwa/helper",
  "version": "1.4.5",
  "description": "Helper classes",
  "repository": "git://github.com/calamus-pwa/helper.git",
  "type": "commonjs",
  "main": "./index.js",
  "types": "./index.d.ts",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "clean-lib": "rimraf ./dist",
    "copypackage": "cp -rf package.json dist",
    "build": "npm run clean-lib && tsc && npm run copypackage",
    "build-test": "npm run clean-lib && tsc && npm run copypackage"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
